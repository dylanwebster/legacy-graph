/** Maximum zoom for the bundled Natural Earth basemap.
 *  Used by both the maplibregl.Map ctor and fitBounds so the basemap zoom cap
 *  and the fly-to cap can never drift apart. */
export const BASEMAP_MAX_ZOOM = 8;
