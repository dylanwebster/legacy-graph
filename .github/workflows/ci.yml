name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# CI only reads the repo. Pin the token down rather than inheriting the
# repository default, which is read/write in some configurations.
permissions:
  contents: read

jobs:
  # ── Backend: type-check + unit tests ─────────────────────────────────────
  unit:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci

      - name: Type-check backend
        run: npm run build

      - name: Lint backend
        run: npm run lint

      - name: Run Vitest
        run: npm test -- --reporter=verbose --run

  # ── Frontend: type-check + build ─────────────────────────────────────────
  build-client:
    name: Build client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install client dependencies
        run: cd client && npm ci

      - name: Lint client
        run: cd client && npm run lint

      - name: Build client (type-check + Vite)
        run: cd client && npm run build

      - name: Run client Vitest
        run: cd client && npm test -- --run

  # ── E2E: Playwright (requires both jobs above to pass first) ──────────────
  e2e:
    name: E2E tests
    runs-on: ubuntu-latest
    needs: [unit, build-client]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci

      - name: Install client dependencies
        run: cd client && npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Create empty .env (required by npm start --env-file=.env)
        run: touch .env

      - name: Run Playwright E2E tests
        run: npm run test:e2e
        env:
          CI: true

      - name: Upload Playwright report
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload test results (traces/screenshots on failure)
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: test-results
          path: test-results/
          retention-days: 7
