## What this changes

<!-- What behaviour is different after this PR, and why. Link the issue: Fixes #123 -->

## How it was tested

<!-- Which tests you added or changed, and what you exercised by hand. -->

## Checklist

- [ ] A failing test was written before the implementation (TDD)
- [ ] `npm test` passes
- [ ] `npm run test:e2e` passes
- [ ] `npm run lint` and `cd client && npm run lint` pass with zero errors
- [ ] `npm run build` type-checks (backend and client)
- [ ] `SPECIFICATION.md` updated if behaviour or schema changed
- [ ] `PROGRESS.md` updated if this completes or starts a phase
- [ ] No real family data in the diff, tests, or screenshots
